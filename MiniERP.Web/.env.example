# Local dev: leave empty — Vite proxies /api to the API (see vite.config.ts)
# VITE_API_URL=

# Production: API origin only, no trailing slash, no /api suffix
# VITE_API_URL=http://51.20.51.132:8080
